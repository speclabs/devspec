---
name: "devspec.codebase-structure"
description: "Create or update structured devspec foundation codebase structure, boundaries, and multi-repo configuration."
argument-hint: "Describe repository layout, module or integration boundaries, ownership, or multi-repo setup"
agent: "devspec.codebase-structure"
---

Create or update `devspec/foundation/codebase-structure.md` with selective repo trees, structured boundary tables, sources, confidence, and work guidance.

Required user input:
${input:codebaseStructureInput:Describe repository layout, module or integration boundaries, ownership, or multi-repo setup}
